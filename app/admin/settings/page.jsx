'use client';

export const metadata = { title: "Settings | Admin Dashboard" };

const Settings = () => {
    return (
        <div>
            Settings
        </div>
    );
};

export default Settings;